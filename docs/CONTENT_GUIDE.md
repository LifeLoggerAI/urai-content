# Content Guide

1. Add or edit JSON files under `content/`.
2. Ensure each public item includes id/title/slug/summary/status/visibility/updatedAt/tags/relatedSystem/sections.
3. Run `npm run validate:content`.
4. Run `npm run content:index`.
5. Commit both source and generated index.
