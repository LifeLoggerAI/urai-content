# Contributing

- Keep URAI tone cinematic, calm, privacy-first, non-clinical.
- Use `Council` terminology.
- Mark demos as `status: demo` and `visibility: demo`.
- Run `npm run check` before opening a PR.
